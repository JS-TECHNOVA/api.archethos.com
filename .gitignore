# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Virtualenv
.venv/
venv/
env/

# Django
*.log
db.sqlite3
db.sqlite3-journal
/media/
/staticfiles/
/static_root/

# Environment
#
# Deny-by-default: every .env* is ignored unless explicitly excepted, so a new
# .env.staging cannot be committed by accident. Only templates are excepted,
# and they must never contain a real value.
.env
.env.*
!.env.example
!.env.production.example

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
